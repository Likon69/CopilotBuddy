# Styx.WoWInternals.UI

Contains WoW UI related classes.

## Contents

- [Backdrop Class](#backdrop-class)

---

### Backdrop Class

**Inheritance:** System.Object → Styx.WoWInternals.UI.Backdrop

```csharp
public class Backdrop
```

A backdrop.

#### Backdrop Properties

- **`BackgroundFile Property`**
  ```csharp
  public string BackgroundFile { get; set; }
  ```
  Gets or sets the background file.

- **`EdgeFile Property`**
  ```csharp
  public string EdgeFile { get; set; }
  ```
  Gets or sets the edge file.

- **`EdgeSize Property`**
  ```csharp
  public int EdgeSize { get; set; }
  ```
  Gets or sets the size of the edge.

- **`InsetBottom Property`**
  ```csharp
  public int InsetBottom { get; set; }
  ```
  Gets or sets the inset bottom.

- **`InsetLeft Property`**
  ```csharp
  public int InsetLeft { get; set; }
  ```
  Gets or sets the inset left.

- **`InsetRight Property`**
  ```csharp
  public int InsetRight { get; set; }
  ```
  Gets or sets the inset right.

- **`InsetTop Property`**
  ```csharp
  public int InsetTop { get; set; }
  ```
  Gets or sets the inset top.

- **`Tile Property`**
  ```csharp
  public bool Tile { get; set; }
  ```
  Gets or sets a value indicating whether the tile.

- **`TileSize Property`**
  ```csharp
  public int TileSize { get; set; }
  ```
  Gets or sets the size of the tile.

---
